{
	"name": "v4-email-hook",
	"version": "0.0.0",
	"private": true,
	"scripts": {
		"deploy": "wrangler deploy --keep-vars",
		"dev": "wrangler dev",
		"start": "wrangler dev",
		"test": "vitest",
		"cf-typegen": "wrangler types"
	},
	"devDependencies": {
		"@cloudflare/vitest-pool-workers": "^0.8.19",
		"@types/node": "^24.0.13",
		"typescript": "^5.5.2",
		"vitest": "~3.2.0",
		"wrangler": "^4.24.3"
	}
}
